package org.example.Model.Members.Fields;

/**
 * Интерфейс добавления даты
 * Реализован принцип разделения интерфейсов
 * Данный интерфейс может быть использован не только для создания класса Birthday
 */
public interface AdderDateImpl {
    Birthday addDate();
}
